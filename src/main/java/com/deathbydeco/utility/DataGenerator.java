package com.deathbydeco.utility;

import java.io.File;
import java.net.URISyntaxException;
import java.util.ArrayList;

/**
 * Created by Harry on 3/26/2017.
 */
public class DataGenerator {



    public static ArrayList<DataSet> generateData(){
        /*ArrayList<DataSet> data = new ArrayList<>();

        // All the folders containing data should be put in here
        String[] dataFolders = {"bulleyes", "Central Island", "Smiley Face"};
        for (String dataFolder: dataFolders) {
            try {
                File folder = FileHandling.getFile("/datasets/" + dataFolder + "/");
                File[] listOfFiles = folder.listFiles();
                for (File file : listOfFiles) {
                    if (file.isFile()) {

                        data.add(new DataSet(file));
                    }
                }
            } catch (URISyntaxException e) {
                e.printStackTrace();
            }
        }*/
        return  null;
    }

}
